import * as z from 'zod/v4';
const task=z.object({command:z.string().min(1),cwd:z.string().optional(),timeoutMs:z.number().int().positive().optional()});
const projection=z.object({include:z.array(z.string()).optional(),exclude:z.array(z.string()).optional(),map:z.array(z.object({from:z.string(),to:z.string()})).optional()});
const permissions=z.object({inspect:z.boolean().optional(),review:z.boolean().optional(),commit:z.boolean().optional(),push:z.boolean().optional(),forcePush:z.boolean().optional(),createPr:z.boolean().optional(),mergePr:z.boolean().optional(),release:z.boolean().optional()});
const branch=z.object({strategy:z.enum(['git-flow','github-flow','trunk','custom']).optional(),main:z.string().optional(),develop:z.string().optional(),allowed:z.array(z.string()).optional()});
const release=z.object({enabled:z.boolean().optional(),tagPrefix:z.string().optional(),rules:z.record(z.string(),z.enum(['major','minor','patch','none'])).optional()});
export const profileSchema=z.object({remote:z.string().min(1),provider:z.enum(['github','gitlab','bitbucket','forgejo','generic']).optional(),targetBranch:z.string().optional(),visibility:z.enum(['public','private','internal']).optional(),history:z.enum(['preserve','snapshot']).optional(),projection:projection.optional(),permissions:permissions.optional(),review:z.object({tasks:z.array(z.string()).optional(),maxDiffBytes:z.number().positive().optional(),blockOn:z.array(z.enum(['critical','high','medium','low'])).optional()}).optional(),branch:branch.optional(),release:release.optional(),tasks:z.record(z.string(),task).optional(),preDelivery:z.array(z.string()).optional(),postDelivery:z.array(z.string()).optional(),requireClean:z.boolean().optional(),requireConventionalCommits:z.boolean().optional()});
export const configSchema=z.object({version:z.literal(1),defaultProfile:z.string().optional(),profiles:z.record(z.string(),profileSchema),tasks:z.record(z.string(),task).optional()});
