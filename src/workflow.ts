import type { BranchConfig } from './types.js'; import { matches } from './config/glob.js';
export function validateBranch(branch:string,cfg:BranchConfig={}){ const strategy=cfg.strategy??'custom', main=cfg.main??'main',develop=cfg.develop??'develop'; let allowed=true,reason=''; if(strategy==='git-flow') {allowed=branch===main||branch===develop||matches(branch,['feature/**','release/**','hotfix/**']);reason='git-flow permits main, develop, feature/*, release/* and hotfix/*';} else if(strategy==='github-flow'){allowed=branch===main||!branch.startsWith('release/');reason='github-flow expects short-lived branches targeting main';} else if(strategy==='trunk'){allowed=branch===main||matches(branch,cfg.allowed??['feature/**','fix/**','chore/**']);reason='trunk policy';} else if(cfg.allowed?.length){allowed=matches(branch,cfg.allowed)||branch===main;reason='custom allowed patterns';} return {allowed,strategy,reason}; }
