import { git } from '../git/git.js'; import { recommendBump,bumpVersion } from '../release/semver-lite.js'; export async function releaseInspect(repo:string,currentVersion:string,rules={}){const last=(await git(repo,['describe','--tags','--abbrev=0'],true)).stdout.trim();const range=last?`${last}..HEAD`:'HEAD';const log=(await git(repo,['log','--format=%B%x00',range],true)).stdout;const messages=log.split('\x00').map(x=>x.trim()).filter(Boolean);const rec=recommendBump(messages,rules);return {currentVersion,lastTag:last||null,commits:messages.length,...rec,nextVersion:bumpVersion(currentVersion,rec.bump)}}
