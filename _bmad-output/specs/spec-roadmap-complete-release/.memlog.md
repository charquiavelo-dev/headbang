---
topic: HEADBANG roadmap-complete 2.0.0 release
goal: Ship every roadmap capability in one self-hosted release
updated: 2026-08-19T12:44
---

- (direction by user) Implement every capability currently described in HEADBANG_ROADMAP_CODEX.md in one release; do not defer items to later versions.
- (direction by user) Separate building reusable HEADBANG capabilities from using HEADBANG in this repository, then dogfood HEADBANG to validate, push, and publish itself to npm.
- (correction by user) The missing develop failure occurred in another consumer repository; do not create or restore develop in the HEADBANG source repository.
- (constraint by user) Git Flow support must be native ordinary Git inside HEADBANG; never install or invoke an external git-flow executable.
- (decision) Use version 2.0.0 because the unified release includes the roadmap public plugin API and other breaking-boundary capabilities.
- (capability) Complete release lifecycle: plan, version adapters, notes/changelog, remote synchronization, deliberate branch/tag push, provider releases, journal, partial-state reporting, idempotency, prereleases, and real MCP integration.
- (capability) Complete native Git Flow including explicit idempotent initialization of missing configured develop from main without recovering deleted history.
- (capability) Complete provider-neutral collaboration: GitHub Flow lifecycle, change requests, branch protection, merge strategies, structured review findings, approved review publishing, delivery sets/channels, and capability discovery.
- (capability) Complete onboarding and reliability: init/presets, versioned config validation/migration, credential diagnostics, optional scanner adapters, locks, crash repair, resume, platform hardening, special Git object handling, and large-repo safeguards.
- (capability) Complete extensibility: public plugin contracts, monorepo release modes, provider-neutral package publishing with npm first, supply-chain artifacts, and MCP resources.
- (constraint) CLI and MCP call shared domain services; mutations are least-privilege, explicit, confirmable, idempotent, and never silently destructive; JSON uses a stable response envelope.
- (acceptance) All advertised lifecycles have real end-to-end tests, documentation, Windows coverage, and npm pack verification; HEADBANG then uses its own config and commands to push main and publish headbang-mcp@2.0.0.
- (event) Scope preservation pass completed: all roadmap headings 1 through 50 and the user corrections are represented in the master release contract.
- (constraint by user) No implementation commit may be created before its relevant automated tests pass; the final release commit and push happen only after the complete validation suite is green.
